<mah:MetroWindow x:Class="RateCalc.calcSaveDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 Title="Input" Height="200" Width="400"
                 WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
                 ShowTitleBar="False"
                ShowCloseButton="False"
                ShowMinButton="False"
                ShowMaxRestoreButton="False"
                BorderThickness="0"
                WindowStyle="SingleBorderWindow"
                AllowsTransparency="False"
                 Background="{StaticResource SemiDarkBackground}">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label x:Name="lblQuestion" Grid.Row="0" 
               Foreground="White" FontSize="12" Margin="0,0,0,10"/>

        <TextBox x:Name="txtResponse" Grid.Row="1" 
                 Background="{StaticResource SecondDarkBackground}"
                 Foreground="White" FontSize="12" Padding="5"
                 BorderBrush="{StaticResource LightSecondDarkBackground}"
                 KeyDown="txtResponse_KeyDown"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btnOK" Content="OK" Width="75" Height="25" 
                    Margin="0,0,10,0" IsDefault="True"
                    Background="{StaticResource SemiDarkBackground}"
                    Foreground="White" Click="btnOK_Click"/>
            <Button x:Name="btnCancel" Content="Cancel" Width="75" Height="25"
                    IsCancel="True" 
                    Background="{StaticResource SemiDarkBackground}"
                    Foreground="White" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</mah:MetroWindow>